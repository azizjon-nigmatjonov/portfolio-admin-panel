# Skip audit during install (avoids SSL/cert errors on some networks)
audit=false

# If you still see "Exit handler never called", try:
# 1. Use Node LTS (20 or 22): nvm use 20
# 2. Or: Remove node_modules + package-lock.json, then npm i
# 3. Or use pnpm: npm i -g pnpm && pnpm i
